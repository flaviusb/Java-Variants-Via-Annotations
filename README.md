Java Variants Via Annotations
-----------------------------

Variants are a lesser form of sum types. Java does not have them. We can encode them in a structured way, which can be automated through annotations.
